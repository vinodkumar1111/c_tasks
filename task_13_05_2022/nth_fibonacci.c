#include <stdio.h>
int nth_fibonacci(int first_element, int second_element, int array, int len)
{
    
}
int main()
{

    int array[] = {1, 2, 3, 4, 5, 6, 7, 8};
    int first_element, second_element;
    int len = sizeof(array) / sizeof(int);
    nth_fibonacci(first_element, second_element, array, len);
}